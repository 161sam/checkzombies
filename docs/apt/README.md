# checkzombies APT Repository

This directory is published via GitHub Pages and contains a signed Debian/Ubuntu APT repository.

Users should install the keyring to `/usr/share/keyrings/` and use `signed-by=...` in the source entry.
No `apt-key` usage.
