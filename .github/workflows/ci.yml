name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: sudo apt install bats-core shellcheck
    - run: make test lint
    - run: make deb    # Debian Package bauen
